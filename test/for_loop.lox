var n = 10; // number of terms in sequence
var a = 0; var b = 1; // initialize the first two terms
var c = 0;

for(var i = 0; i < n; i = i + 1) {
    print a;
    
    // Compute the next term in the sequence
    c = a + b;
    a = b;
    b = c;
}