fun fib(n) {
  if (n < 2) return n;
  return fib(n - 2) + fib(n - 1);
}

var start = clock();
print fib(15);
print "Miliseconds: ";
print clock() - start;

fun add(x,y,z) {
  return x + y + z;
}

print add(1,2,3);

fun loop(x){
  for(var y = 0; y < x; y = y + 1){
    print y;
  }
}

loop(5);